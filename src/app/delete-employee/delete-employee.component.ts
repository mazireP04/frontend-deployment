import { Component } from '@angular/core';

@Component({
  selector: 'app-delete-employee',
  templateUrl: './delete-employee.component.html',
  styleUrl: './delete-employee.component.scss'
})
export class DeleteEmployeeComponent {

  // -- delete button on the employee list in assign form? --  form to select id and name of employee -- single options
  // click delete and asks if wanna delete this name, id, designation, department
  // if yes -- send id for marking as deleted
  // refresh and the deleted name n ids are no more shown
  // the assigned items to that deleted employees are marked avaialable.....
  // 
}
